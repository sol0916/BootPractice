CREATE TABLE TOPIC(
	TOPIC_NUM INT PRIMARY KEY AUTO_INCREMENT, # PK (자동증가값)
	TOPIC_REGDATE VARCHAR(20), #등록일 (문자형 시간)
    TOPIC_ID VARCHAR(20), 				   #아이디
    TOPIC_TITLE VARCHAR(50),			   #제목
    TOPIC_CONTENT VARCHAR(5000)			   #내용
);